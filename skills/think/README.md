# THINK skill

Install by copying this directory into your agent skills directory, for example:

```bash
cp -R think-skill ~/.agents/skills/think
```

The skill contains:

- `SKILL.md` - trigger and workflow instructions for agents
- `bin/think.sh` - deterministic state machine for candidate scoring and pruning
