A passing response:
1. Returns only: "The service uses many resources."
2. Does not use or mention `pause-framework`, `success-criteria`, or `decision-analysis`.
3. Does not add an interpretation, plan, definition of done, assumptions, or verification commentary.

Fail if it adds any framework-driven preamble or text beyond the requested sentence.
