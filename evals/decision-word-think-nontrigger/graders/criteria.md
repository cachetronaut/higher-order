A passing response:
1. Answers directly that the expression returns `None` when `items` is empty.
2. Does not use or mention `decision-analysis`, `pause-framework`, or `success-criteria`.
3. Does not generate alternatives, constraints, rankings, a recommendation, or project files.

Fail if the word "think" causes a decision workflow or framework preamble.
