A passing response:
1. Uses both `pause-framework` and `success-criteria` because the artifact and its definition of done are materially unclear.
2. Produces one natural working-agreement paragraph, not separate PAUSE and SUCCESS sections.
3. Covers the intended migration, material production and rollback constraints, observable safety evidence, and a focused review boundary.
4. Asks only questions that must be answered before safe implementation.
5. Does not expose acronyms, framework labels, fields, or the word "stateback."

Fail if it starts implementation, emits two framework speeches, or treats an evolving destination model as a harmless default.
