# Higher Order routing evaluations

Behavioral cases for `claude plugin eval` covering selective skill activation and natural output. Each case contains a user prompt and criteria for a passing response.

Run the full suite from the repository root:

```bash
claude plugin eval . --runs 3
```

Claude Code currently labels plugin evaluations as early access. If the command is unavailable for the active profile, retain these cases and run them when access is enabled.
