A passing response:
1. Answers `uv` directly because an established repository rule determines the choice.
2. Does not use or mention `decision-analysis`, `pause-framework`, or `success-criteria`.
3. Does not compare alternatives, score options, or ask for more context.

Fail if it treats the settled policy as an open decision.
